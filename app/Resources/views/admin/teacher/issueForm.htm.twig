<div id="form-data" class="form form-wrap" style="">
    {{ form_start(form,{'attr':{'id':'newForm'}}) }}
    <h2>Basic Details</h2>

    <div>
        <div class="column">

            <div class="form-row">
                <div class="form-widget">
                    {{ form_row(form.book, { 'attr': {'class': 'slim-select'} }) }}
                </div>
                <div class="form-errors"></div>
            </div>
        </div>
        <div class="column">
            <div class="form-row">
                <div class="form-widget">
                    {{ form_row(form.student, { 'attr': {'class': 'slimSelect'} }) }}
                </div>
                <div class="form-errors"></div>
            </div>

        </div>
        <div class="button-row">
            <input id="new" type="submit" value="Issue & Continue">

            <a href="{{ path('my-accepted-assigned-books-teacher') }}" id="restore">
                Cancel </a>
        </div>
        {{ form_end(form) }}
    </div>

</div>

<script type="text/javascript">
    new SlimSelect({
        select: '.slim-select'
    })
    new SlimSelect({
        select: '.slimSelect'
    })
</script>